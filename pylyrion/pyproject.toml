[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "pylyrion"
version = "0.0.0"
description = "High-level Python client for Logitech Media Server / Lyrion"
requires-python = ">=3.14"
